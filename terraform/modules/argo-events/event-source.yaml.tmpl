apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: gitea
  namespace: argo
spec:
  service:
    ports:
      - port: 12000
        targetPort: 12000
  webhook:
    gitea:
      endpoint: /push
      port: 12000
      method: POST
      url: "${webhook_url}"
