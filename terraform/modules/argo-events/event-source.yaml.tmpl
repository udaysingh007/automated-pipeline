apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: gitea
  namespace: "${namespace}"
spec:
  serviceAccountName: argo-workflows-server
  service:
    ports:
      - port: 12000
        targetPort: 12000
  webhook:
    gitea:
      endpoint: /push
      port: "12000"
      method: POST
